// @flow

export type IsUrl = (url: string) => boolean;
// TODO: This isn't working
// export type GetPwd = (src: string|string[]) => string|string[];
export type GetPwd = (src: string|string[]) => any;
