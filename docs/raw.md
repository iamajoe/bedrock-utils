# Bedrock: Utils > Raw

Module to perform raw commands.<br>

=========

## Configure

### Config file parameters
```toml
[[raw]]
command = "" # required
args = [""]
order = 0
env = "both"
sys = "all"
```
