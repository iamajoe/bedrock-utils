{
  "name": "bedrock_utils",
  "version": "0.0.1",
  "scripts": {
    "go-install": "cd cmd; go get . ; cd ..",
    "build": "rm -rf bin/bedrock bin/external; cd cmd; go build . ; mv cmd ../bin/bedrock; cd ..; mv modules/external bin/external",

    "example-build": "bin/bedrock build test/example_build/build.toml",
    "example-es6": "bin/bedrock build test/example_es6/build.toml",
    "example-server": "bin/bedrock run test/example_server/server.toml",

    "example-container-vagrant-vars": "export BEDROCK_VAGRANT_IP=192.168.33.11; export BEDROCK_VAGRANT_PUBLIC_IP=192.168.2.100; export BEDROCK_VAGRANT_PROJECT_PATH=$(pwd)/test/example_container; export BEDROCK_VAGRANT_BEDROCK_PATH=$(pwd)",
    "example-container-init": "bin/bedrock init test/example_container/server.toml",
    "example-container-run": "bin/bedrock run test/example_container/server.toml",
    "example-container-stop": "bin/bedrock stop test/example_container/server.toml",
    "example-container-destroy": "bin/bedrock destroy test/example_container/server.toml",

    "example-test-build-times-go": "SECONDS=0; npm run example-build prod; echo \"[example-go] $SECONDS secs\" >> log/times.log",
    "example-test-build-times-node": "SECONDS=0; pushd test/example/node_build_comparison; npm run build prod; popd; echo \"[example-node] $SECONDS secs\" >> log/times.log",
    "example-test-build-times": "echo \"[example] Start time test\"; for i in {1..3}; do npm run example-test-build-times-go; npm run example-test-build-times-node; done; echo \"[example] Time test ended\""
  }
}
